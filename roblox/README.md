# Roblox版(Rojoプロジェクト)

これまでブラウザ(`index.html`)で作っていたゲームを、Robloxに移行して作り直すためのプロジェクトです。
Robloxのスクリプト(Luau)はこのフォルダにファイルとして置き、[Rojo](https://rojo.space)という
無料ツールでRoblox Studioとリアルタイム同期しながら開発します。

## はじめに(1回だけ行う準備)

### 1. Roblox Studioをインストール
[roblox.com](https://www.roblox.com) でアカウントを作り(未登録なら)、Roblox Studioをダウンロード・
インストールします(Windows/Mac対応、無料)。

### 2. Rokit(ツール管理ソフト)をインストール

ターミナル(Mac)またはPowerShell(Windows)で実行します。

**Mac:**
```sh
curl -sSf https://raw.githubusercontent.com/rojo-rbx/rokit/main/scripts/install.sh | bash
```

**Windows(PowerShell):**
```sh
Invoke-RestMethod https://raw.githubusercontent.com/rojo-rbx/rokit/main/scripts/install.ps1 | Invoke-Expression
```

### 3. このフォルダ(`roblox/`)でRojoをインストール

ターミナルでこの`roblox`フォルダに移動してから実行します。

```sh
rokit add rojo-rbx/rojo
rokit install
```

### 4. Roblox Studio用のRojoプラグインを入れる

```sh
rojo plugin install
```

## 毎回の開発の流れ

1. `roblox` フォルダでターミナルを開き、以下を実行してサーバーを起動する

   ```sh
   rojo serve
   ```

2. Roblox Studioで新規プレイス(Baseplateなど)を開く
3. Studio上部のツールバーから **Rojo** プラグインのボタンを押し、パネルの **Connect** を押す
4. 同期されたら、Studio上部の **▶(再生)** ボタンを押してテスト再生する
5. 緑色のブロックが出現し、出力(View → Output)に「つながったよ!」と表示されれば接続成功

## 現在の状況

- 接続確認 ✅ 完了
- 雑草ぬき最小ロジック(近づいてぬく→ポイント増減) ✅ 完了・動作確認済み
- レベルの仕組み ✅ 追加(ブラウザ版と同じLv.1〜3、レベルが足りない雑草はぬけない、
  おはなを抜くとレベルダウンもあり)
- レベルアップ・ロック中の案内は画面右上の通知(`StarterGui:SendNotification`)で表示
- 雑草の見た目を種類ごとに変更 ✅(小=細い葉っぱ、中・大=丸い茂み、おはな=茎+光る花の頭)
- 雑草・おはなはランダムな位置に生え、抜くと数秒後に別の場所にまた生える ✅
- 画面左上にレベル・ポイント・進捗バーのカスタムHUD ✅
  (レベルのルールは`src/ReplicatedStorage/GameConfig.lua`にまとめ、サーバー・
  クライアントの両方から参照する形に整理)
- 進捗の保存(DataStore) ✅ 退出しても、また入ったときにポイント・レベルが復元される
  (Robloxに公開した本番環境で動作確認済み)
- 雑草を**Lv.1〜32、全32種類**に拡張 ✅(たんぽぽ→…→ビル→電波塔まで、そのレベルに
  なったら抜けるようになる)
- 「抜いてはいけないもの」を**5レベルごとに新しい種類が増える、全10種類**に拡張 ✅
  (おはな→きのこ→郵便ポスト→…→自転車。全種類が最初から世界に置かれているが、
  レベルが足りないと反応しない)
- 道具を**8種類**追加、レベルが上がると自動的に持ち替わる ✅(軍手→虫眼鏡→草刈り鎌→
  熊手→剪定ばさみ→電動草刈り機→チェーンソー→クレーン付き重機。手に溶接して表示)
- 雑草・抜いてはいけないもの・道具の一覧は`src/ReplicatedStorage/GameConfig.lua`に
  すべてまとめてあるので、種類や見た目の数値を増減させたいときはここを編集する
- Creator Store(クリエーターストア)のモデルを雑草の見た目として使えるように対応 ✅
  (たんぽぽ = アセットID `14711174110`)。`GameConfig.lua`の該当雑草に`assetId`を
  追加するだけで、図形の代わりにそのモデルが使われる。**読み込んだアセットの中に
  Script/LocalScript/ModuleScriptが含まれていたら自動的に検出・削除する**仕組みを
  実装済み(無料モデルへの不審なスクリプト混入対策)。削除が起きた場合はサーバーの
  出力(またはF9の「サーバー」タブ)に警告として表示される
- 未対応: ロック中の雑草を灰色にする(見ただけでロック中と分かるようにする)、
  お片づけステージ、年齢に応じた見た目変化、抜く時のアニメーション、
  効果音・BGM、実績バッジ、大きなオブジェクト同士が重ならないようにする配置調整

## 進捗保存(DataStore)の動作確認について

DataStoreはデフォルトではStudio上のテストプレイ(▶ボタン)では使えません。本来は
ゲーム設定の「セキュリティ」タブにある「Studio内でのAPIサービスへのアクセスを
有効にする」をONにすれば使えますが、**最近のStudioではこの項目が
ファイル/ホームタブから見当たらないことがある**(UIが変更中の可能性)。

見つからない場合は、無理に探さず**「Robloxに公開」してから、実際に公開後のゲームで
動作確認する**のが早い(公開済みの本番サーバーではこの設定なしでDataStoreが動く)。
うまく保存されない場合は、公開後のゲームをF9キーで開発者コンソールを開き、
「サーバー」タブでエラー・警告(`進捗の保存に失敗しました`など)を確認する。

`rojo serve` を起動してStudioと接続したままにしておけば、このリポジトリを `git pull` するだけで
最新のスクリプトが自動的にStudio側へ同期されます。**ただし、新しく追加されたファイルが同期
されないことがあるので、その場合は `rojo serve` を再起動してから、StudioのRojoパネルで
Disconnect→Connectし直してください。**
