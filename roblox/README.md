# Roblox版(Rojoプロジェクト)

これまでブラウザ(`index.html`)で作っていたゲームを、Robloxに移行して作り直すためのプロジェクトです。
Robloxのスクリプト(Luau)はこのフォルダにファイルとして置き、[Rojo](https://rojo.space)という
無料ツールでRoblox Studioとリアルタイム同期しながら開発します。

## はじめに(1回だけ行う準備)

### 1. Roblox Studioをインストール
[roblox.com](https://www.roblox.com) でアカウントを作り(未登録なら)、Roblox Studioをダウンロード・
インストールします(Windows/Mac対応、無料)。

### 2. Rokit(ツール管理ソフト)をインストール

ターミナル(Mac)またはPowerShell(Windows)で実行します。

**Mac:**
```sh
curl -sSf https://raw.githubusercontent.com/rojo-rbx/rokit/main/scripts/install.sh | bash
```

**Windows(PowerShell):**
```sh
Invoke-RestMethod https://raw.githubusercontent.com/rojo-rbx/rokit/main/scripts/install.ps1 | Invoke-Expression
```

### 3. このフォルダ(`roblox/`)でRojoをインストール

ターミナルでこの`roblox`フォルダに移動してから実行します。

```sh
rokit add rojo-rbx/rojo
rokit install
```

### 4. Roblox Studio用のRojoプラグインを入れる

```sh
rojo plugin install
```

## 毎回の開発の流れ

1. `roblox` フォルダでターミナルを開き、以下を実行してサーバーを起動する

   ```sh
   rojo serve
   ```

2. Roblox Studioで新規プレイス(Baseplateなど)を開く
3. Studio上部のツールバーから **Rojo** プラグインのボタンを押し、パネルの **Connect** を押す
4. 同期されたら、Studio上部の **▶(再生)** ボタンを押してテスト再生する
5. 緑色のブロックが出現し、出力(View → Output)に「つながったよ!」と表示されれば接続成功

## 現在の状況

- 接続確認 ✅ 完了
- 雑草ぬき最小ロジック(触れるだけでぬける→ポイント増減) ✅ 完了・動作確認済み
  (以前は「E」ボタンで抜く方式だったが、触れるだけで自動的に抜ける方式に変更した)
- レベルの仕組み ✅ 追加(ブラウザ版と同じLv.1〜3、レベルが足りない雑草はぬけない、
  おはなを抜くとレベルダウンもあり)
- レベルアップ・ロック中の案内は画面右上の通知(`StarterGui:SendNotification`)で表示
- 雑草の見た目を種類ごとに変更 ✅(小=細い葉っぱ、中・大=丸い茂み、おはな=茎+光る花の頭)
- 雑草・おはなはランダムな位置に生え、抜くと数秒後に別の場所にまた生える ✅
- 画面左上にレベル・ポイント・進捗バーのカスタムHUD ✅
  (レベルのルールは`src/ReplicatedStorage/GameConfig.lua`にまとめ、サーバー・
  クライアントの両方から参照する形に整理)
- 進捗の保存(DataStore) ✅ 退出しても、また入ったときにポイント・レベルが復元される
  (Robloxに公開した本番環境で動作確認済み)
- 雑草を**Lv.1〜32、全32種類**に拡張 ✅(たんぽぽ→…→ビル→電波塔まで、そのレベルに
  なったら抜けるようになる)
- 「抜いてはいけないもの」を**5レベルごとに新しい種類が増える、全10種類**に拡張 ✅
  (おはな→きのこ→郵便ポスト→…→自転車。全種類が最初から世界に置かれているが、
  レベルが足りないと反応しない)
- 道具を**8種類**追加、レベルが上がると自動的に持ち替わる ✅(軍手→虫眼鏡→草刈り鎌→
  熊手→剪定ばさみ→電動草刈り機→チェーンソー→クレーン付き重機。手に溶接して表示)
- 雑草・抜いてはいけないもの・道具の一覧は`src/ReplicatedStorage/GameConfig.lua`に
  すべてまとめてあるので、種類や見た目の数値を増減させたいときはここを編集する
- Creator Store(クリエーターストア)のモデルを雑草の見た目として使えるように対応 ✅
  現在、たんぽぽ・しろつめくさ・ススキ・いばら・低木・若木・切り株・竹・
  蔦が絡まったフェンス・街路樹・大きな木・放置自転車の雑草・電柱に絡まる蔦・
  廃屋の庭・一軒家・マンション・ビル・電波塔の**18種類**にCreator Storeのモデルを
  設定済み。`GameConfig.lua`の該当雑草に`assetId`を追加/削除するだけで、モデルと
  図形を切り替えられる。**読み込んだアセットの中にScript/LocalScript/ModuleScript
  が含まれていたら自動的に検出・削除する**仕組みを実装済み(無料モデルへの不審な
  スクリプト混入対策)。削除が起きた場合はサーバーの出力(またはF9の「サーバー」
  タブ)に警告として表示される
- レベルの上限を**999**に変更 ✅(雑草・道具・おはなの種類は32段階までしか無いが、
  レベル自体はそれ以上も上がり続ける。32を超えると一番上の雑草・道具のまま)
- 「アセット探しが面倒」という要望を受けて、**Creator Storeのアセットに頼らず
  複数パーツを組み合わせて見た目を作る方式(花・草の束・建物)を導入** ✅
  (`assetId`が設定されていればそちらを優先するが、無くても単なる棒/丸/箱ではなく
  それっぽい形になる。今後は資産探しをせず、こちらの方式を基本にしていく想定)
- **雑草・おはなをレベル順に少しずつ出すのではなく、最初から全種類を広いフィールドに
  混ぜて出すように変更** ✅(低いレベルの雑草ほどよく出るよう重み付けしつつ、まだ
  抜けないレベルの高い雑草・おはなも見えている(触れても反応しないだけ)。これに伴い、
  雑草がどのレベルで出現するかをアセットの有無で制限する仕組みは撤廃した)
- **フィールドを拡大**(半径45→**450**、面積で約100倍) ✅
- 地面を緑の芝生(`Enum.Material.Grass`)に ✅。雑草の高さ計算をRaycastの
  毎回判定から「芝生の上面のYを1回だけ調べて使い回す」方式に変更し、**地面ができた
  直後のタイミングで雑草が埋まってしまう不具合を修正** ✅
- **街を碁盤の目状の道路網にして、フィールドの外側だけでなく全体に行き渡らせた** ✅
  (これまでは外側にリング状に配置していたため、道路まで遠すぎて見えなかった。
  道路の交差点・区画ごとにビル・駐車中の車を配置)
- **ビルに窓とドアを追加** ✅(高さ・幅に応じて階数・列数を自動計算して並べる)
- **一部の車が道路の上を行ったり来たり走るように** ✅(`Model:PivotTo`でモデル
  ごと動かしているので、パーツがAnchoredのままでも一緒に動く)
- ビル・車・道路はいずれも`CanCollide = true`ですり抜けられない
- **重大な不具合を修正**: `Workspace.StreamingEnabled`をスクリプトから設定しようと
  していたのが原因で、「lacking capability Plugin」というエラーが発生し、それより
  後ろに書かれていた処理(地面・街・雑草の生成)が丸ごと実行されなくなっていた
  (草も道路も何も出ない状態になっていた) ✅ 該当行を削除して修正。
  StreamingEnabled(近くのものだけ描画する設定)を有効にしたい場合は、Studio上部の
  「ゲーム設定」→「ワールド」タブから手動でオンにする必要がある(スクリプトからは
  変更できない仕様)
- **雑草・おはなの数を大幅に増加**(雑草220→**3000**個、おはな40→**200**個) ✅。
  「面積どおり100倍」だと2万本超になってしまい、1雑草あたり複数パーツを使う今の
  見た目や街のパーツ数と合わせるとスマホでは確実に重くなるため、約13倍に留めた
  (これでもまだ少なく感じる場合は増やせる)
- Creator Storeのアセット読み込みで「User is not authorized to access Asset」という
  警告(黄色)が出ることがある。これはIDのアセットが非公開/使用許可なしのため
  読み込めない、というRoblox側の権限の問題で、こちらのコードの不具合ではない。
  読み込みに失敗した雑草は自動的に図形(花・草の束・建物)の見た目にフォールバック
  するので、ゲーム自体は問題なく動く。気になる場合は、そのIDを別のものに
  差し替えるとよい
- **車が恐ろしい速さで走っていた不具合を修正** ✅。位置の計算に
  `sin(角度) × 道路の半分の長さ`を使っていたが、角度を進める速さ(1秒あたりの
  ラジアン数)をそのまま「速度」として渡していたため、実際の移動速度が
  道路の半分の長さ(約510)倍になってしまっていた(時速に直すと数千km級)。
  指定した数値がそのまま実際のスタッド/秒になるよう計算式を修正し、
  24〜36スタッド/秒(それなりに速いが現実的な速さ)に調整した
- **「どの雑草が抜けるのか分かりにくい」を改善** ✅。抜ける雑草を光らせる
  `Highlight`が控えめすぎて、3000本もの雑草の中では埋もれてしまっていたため、
  目立つ緑色・不透明寄りに変更し、他の雑草や建物に隠れていても透けて見える
  `AlwaysOnTop`モードに変更した
- **「抜いてはいけないもの」を10種類→20種類に倍増**(バス停・電柱・銅像・噴水・
  鉄塔・観覧車・橋脚・港のクレーン・飛行機・ロケットを追加、Lv.50〜95で5レベル
  ごとに解禁) ✅
- 雑草を抜いた時・抜けなかった時に、**何の雑草だったかが分かるメッセージ**を表示 ✅
  (「たんぽぽ を ぬいた!(+10pt)」「たんぽぽ は まだ Lv.1にならないと ぬけないよ」等)
- **今のレベルで抜ける雑草だけ、そっと光らせて分かるように** ✅
  (`Highlight`インスタンスで、うっすらとした縁取りの光を追加。目立ちすぎないよう
  透明度高め。各プレイヤーのクライアント側だけの表示なので、レベルが違う人同士でも
  正しく「自分が抜けるものだけ」光る。`src/StarterPlayerScripts/PullableHighlight.client.lua`)
- BGM ✅ / 雑草を抜いた時の効果音の仕組みを追加(**効果音は要:音源のアセットID設定**) ⚠️
  `GameConfig.lua`の`BGM_ASSET_ID`に音源(139410493395489)を設定済み、鳴るように
  なっている。`DEFAULT_PULL_SFX_ID`(抜く効果音のデフォルト)はまだ0(未設定)の
  ため鳴らない。雑草の種類ごとに違う効果音にしたい場合は、`WEED_TIERS`の該当
  エントリに`sfxId = 123456`のように追加すると、そちらが優先される
- 摘んだ花を花束として手に蓄積し、他のプレイヤーを叩けるように ✅
  (画面右下の「たたく」ボタン(PC:クリック/スマホ:タップ)で、近く(6スタッド以内)の
  プレイヤーにダメージを与えられる。見た目の花の本数は最大10本で頭打ちになるが、
  実際に持っている本数(`BouquetCount`属性)は際限なく増え続け、その本数が多いほど
  1回あたりのダメージが大きくなる(ダメージ計算だけ上限40本ぶんで頭打ち、一撃で
  倒しきれないようにするための調整)。画面左上のHUDに現在の花の本数を表示)
- 未対応: ロック中の雑草を灰色にする(見ただけでロック中と分かるようにする)、
  お片づけステージ、年齢に応じた見た目変化、抜く時のアニメーション、
  実績バッジ、花束の本数のセーブ(現状は退出すると0に戻る)、
  BGM・効果音の実際の音源設定(仕組みは用意済み、上記参照)
- 以下の4点を修正 ✅
  - たんぽぽが初期スポーン時に棒のままになることがある不具合
    → アセットの読み込み待ちを`task.spawn`で非同期にしていたことが原因(読み込みが
    終わる前に最初の雑草生成ループが走ってしまっていた)。プレイヤー参加イベントは
    既に安全に接続済みのため、同期的に読み込みを待ってから雑草を生やすように修正
  - Studio上でのテスト再生時に赤いDataStoreエラーが出る
    → `RunService:IsStudio()`で判定し、Studio上では保存処理自体をスキップするように変更
    (`pcall`で囲んでいても、Robloxはエンジン側でエラーをコンソールに出してしまうため)
  - 「E: ぬく」の表示が人物の近くに出て、どの花のものか分かりにくい
    → 当たり判定の半径(`MaxActivationDistance`)を10から4に縮小
  - その場でEを連打すると周りの雑草がまとめて連続で抜けてしまう
    → 雑草・おはなが互いに3.5スタッド以内には生えないようにする間隔調整
    (`MIN_ITEM_SPACING`)を追加、当たり判定の半径縮小と合わせて解消

## 進捗保存(DataStore)の動作確認について

DataStoreはデフォルトではStudio上のテストプレイ(▶ボタン)では使えません。本来は
ゲーム設定の「セキュリティ」タブにある「Studio内でのAPIサービスへのアクセスを
有効にする」をONにすれば使えますが、**最近のStudioではこの項目が
ファイル/ホームタブから見当たらないことがある**(UIが変更中の可能性)。

見つからない場合は、無理に探さず**「Robloxに公開」してから、実際に公開後のゲームで
動作確認する**のが早い(公開済みの本番サーバーではこの設定なしでDataStoreが動く)。
うまく保存されない場合は、公開後のゲームをF9キーで開発者コンソールを開き、
「サーバー」タブでエラー・警告(`進捗の保存に失敗しました`など)を確認する。

`rojo serve` を起動してStudioと接続したままにしておけば、このリポジトリを `git pull` するだけで
最新のスクリプトが自動的にStudio側へ同期されます。**ただし、新しく追加されたファイルが同期
されないことがあるので、その場合は `rojo serve` を再起動してから、StudioのRojoパネルで
Disconnect→Connectし直してください。**
